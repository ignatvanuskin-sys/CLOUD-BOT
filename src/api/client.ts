import type {ApiErrorPayload} from '../types/api';
export class ApiError extends Error{constructor(public status:number,public code:string,message:string,public requestId?:string){super(message);this.name='ApiError'}}
const TOKEN_KEY='cloud-bot-session';export const session={get(){try{return localStorage.getItem(TOKEN_KEY)}catch{return null}},set(token:string){try{localStorage.setItem(TOKEN_KEY,token)}catch{/* unavailable */}},clear(){try{localStorage.removeItem(TOKEN_KEY)}catch{/* unavailable */}}};
type RefreshHandler=()=>Promise<string|null>;let refreshHandler:RefreshHandler|null=null,refreshPromise:Promise<string|null>|null=null;
export function setAuthRefreshHandler(handler:RefreshHandler|null){refreshHandler=handler}
async function request<T>(path:string,options:RequestInit,allowRefresh:boolean):Promise<T>{const token=session.get(),response=await fetch(`/api${path}`,{...options,headers:{...(options.body?{'Content-Type':'application/json'}:{}),...(token?{Authorization:`Bearer ${token}`} :{}),...options.headers}});if(response.ok)return response.json() as Promise<T>;const body=await response.json().catch(()=>({})) as ApiErrorPayload,detail=typeof body.error==='string'?{message:body.error}:body.error||{};if(response.status===401){session.clear();if(allowRefresh&&refreshHandler&&path!=='/auth/telegram'){refreshPromise ||= refreshHandler().finally(()=>{refreshPromise=null});const nextToken=await refreshPromise;if(nextToken)return request<T>(path,options,false)}}throw new ApiError(response.status,detail.code||'request_failed',detail.message||response.statusText,detail.requestId)}
export function api<T>(path:string,options:RequestInit={}){return request<T>(path,options,true)}
